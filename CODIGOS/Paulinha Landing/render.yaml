services:
  - type: web
    name: paulinha-landing
    runtime: node
    rootDir: .
    buildCommand: |
      echo "Diretório atual: $(pwd)"
      echo "Conteúdo do diretório:"
      ls -la
      npm install
      npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    healthCheckPath: /
    autoDeploy: true 